# Team Style Guide

> Auto-generated by ReviewMind on 2026-05-28
> Based on 22 code review feedbacks
> Acceptance rate: 68%

---

# TEAM_STYLE.md

## What we care about
- Keep dashboard metrics tied to stored feedback.
- Use readable markdown for bot comments.
- Prefer explicit configuration constants.
- Use descriptive variable names instead of placeholders.
- Validate environment variables before startup.
- Keep secrets out of tracked example files.
- Use specific exception handling around GitHub API calls.
- Prefer small functions with clear single responsibility.
- Return structured JSON errors from API routes.
- Use context managers for SQLite connections.
- Store team feedback before generating style guides.
- Use clear PR comments with exact fix snippets.
- Guard external API calls with safe fallbacks.
- Avoid committing runtime database files.
- Use repo-specific accepted patterns in prompts.

## What we ignore
- Do not suggest generic comments without code value.
- Avoid asking for documentation-only changes on tiny diffs.
- Do not put real secrets in `.env.example`.
- Avoid broad refactors during focused PR reviews.
- Do not repeat duplicate suggestions on the same PR.
- The GITHUB_WEBHOOK_SECRET value is misleading and unnecessary.
- The GITHUB_WEBHOOK_SECRET value is unclear and potentially misleading.

## Naming conventions
- Use descriptive, meaningful variable names.
- Prefer explicit and clear naming for configuration constants.

## Error handling
- Use specific exception handling around API calls.
- Return structured JSON errors from API routes.

# Team Style Guide

> Auto-generated by ReviewMind on 2026-05-31
> Based on 60 code review feedbacks
> Acceptance rate: 60%

---

# TEAM_STYLE.md

## What we care about
- Consider using a design pattern here.
- This function could be broken into smaller pieces.
- Add an inline comment explaining this line.
- Add logging before and after critical operations.
- Use `enumerate` instead of manual index tracking.
- Add guard clauses to reduce nesting depth.
- Return structured JSON errors from API endpoints.
- Add input validation before database writes.
- Replace magic numbers with named constants.
- Use parameterised queries to prevent SQL injection.
- Add specific exception handling around API calls.
- Use descriptive variable names instead of single letters.

## What we ignore
- Add unit tests for this helper function.
- Add type hints to every local variable.
- Consider adding more documentation to this module.
- Extract repeated logic into helper functions.
- Prefer `pathlib.Path` over `os.path` for file operations.
- Use f-strings instead of string concatenation.
- Add type hints to public function signatures.
- Use context managers for file and database operations.

## Naming conventions
- Use descriptive variable names that convey purpose.
- Avoid single-letter variable names unless in tight loops.

## Error handling
- Always return structured JSON errors from API endpoints.
- Implement specific exception handling around API calls, avoiding broad catches.
